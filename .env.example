# 更新密钥配置
# 请修改以下密钥为您自己的随机字符串
UPDATE_SECRET_KEY=your_secret_key_here_change_me

# 信任的代理服务器IP列表（可选）
# 如果您的服务器在反向代理（Nginx、Cloudflare 等）后面，请配置此项
# 多个IP用逗号分隔，例如：TRUSTED_PROXIES=127.0.0.1,10.0.0.1
# 不配置则默认只使用直连IP（更安全）
# TRUSTED_PROXIES=127.0.0.1

# GitHub 配置（可选，如需自定义）
# GITHUB_REPO=FogMoe/galaxycardgame
# GITHUB_BRANCH=master
# CHECK_INTERVAL=3600
